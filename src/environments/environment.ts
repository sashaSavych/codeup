/**
 * Supabase placeholders (see thesis stack in app/stack.txt).
 * Do not commit real keys — use a local file environment.development.ts (gitignored).
 */
export const environment = {
  production: false,
  supabaseUrl: '',
  supabaseAnonKey: '',
};
